const name = "hitesh";
const repoCount = 50;

console.log(name  + repoCount + "Value");  // not prefer as it is old

console.log(`Hello my name is: ${name} and my repocount is : ${repoCount}`); // interpolation -> modern way

const game = new String("Hello World");
console.log(game);